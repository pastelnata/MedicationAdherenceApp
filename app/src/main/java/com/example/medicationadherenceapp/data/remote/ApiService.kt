package com.example.medicationadherenceapp.data.remote

interface ApiService {
    // we are not implementing this in our app
    // however, this is just to demonstrate how an API call would be made
    /*
    @GET("/health")
    suspend fun healthCheck(): Unit

    @POST("/users/login")
    */
}